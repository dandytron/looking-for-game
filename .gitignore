# Local Claude config — never commit (workspace convention: exclude before going public)
.claude/

# Go
/bin/
/tmp/
*.exe
*.test
*.out

# Env / secrets
.env
.env.*
!.env.example

# Spike output — raw Steam API dumps (may carry a SteamID)
/tests/out/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
